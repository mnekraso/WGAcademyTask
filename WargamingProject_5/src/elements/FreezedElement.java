package elements;

import utilites.Colors;
import utilites.ElementStatus;

public class FreezedElement extends BasicElement {

    public FreezedElement() {
        super(Colors.freezed_color, ElementStatus.FREEZED);
    }
}
