package elements;

import utilites.ElementStatus;

public class MoveableElement extends BasicElement{
    public MoveableElement(String color, ElementStatus elementStatus) {
        super(color, elementStatus);
    }
}
